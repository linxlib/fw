开发一个golang的web开发框架, 主要用于快速开发api接口, 使用"注解"方式进行路由的定义和中间件的使用.

## 要求
- golang版本需要1.27+, 尽量使用新版本的高级特性, 暂不考虑旧版本golang的兼容.
- 尽量少使用第三方依赖, 不使用CGO


## 基础依赖
- `github.com/valyala/fasthttp` 作为底层依赖, 处理底层请求
- `github.com/fasthttp/router` 用于路由
- `github.com/pterm/pterm` 用于在终端输出好看的提示信息
- 使用yaml作为配置文件, 同时可通过环境变量覆盖配置

## 基本功能
- 日志打印, 同时打印到终端和文件(可配置), 对于支持的终端输出带颜色的内容, 对于不支持的终端或文件则打印纯文本
- 路由打印, 树形打印出注册的路由
- 自定义的Context接口, 基于并扩展`*fasthttp.RequestCtx`, 通过更加便利的方法.
- 插件化的中间件设计, 使用Use方法即可使用中间件, 支持多种切面的中间件(前置中间件 后置中间件 全局中间件)
- 全局定义接口返回的http body样式

## 高级功能
- 使用 `inject` 目录下的代码, 可实现依赖注入效果, 启动时注入各个服务, 可在后续的Context中获取
- 使用inject的Invoke实现每个路由在调用时可自动注入所需服务(不仅服务, 参数也可注入)
- 整合`astp`目录的解析器并实现如下:
    - 在Controller的struct上如此注释 `// @Route /api`, 即表示该Controller下的方法被注册在 `/api` 下
    - 在Controller的Method上如此注释 `// @POST /name/:id`, 则该方法被注册为 `/api/name/:id`
    - 在Controller的struct上如此注释 `// @Authorization`, 则该Controller下的路由均会通过 `Authorization` 这个中间件进行处理
    - 在Controller的Method上如此注释 `// @Log`, 则该Method被调用时会经过`Log`中间件的处理(可能为前置或后置)

- 服务启动时, 可通过 `astp`解析所有注册的路由以及对应方法的参数和返回值类型信息, 生成openapi数据